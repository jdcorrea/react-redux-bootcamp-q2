import { createSlice } from "@reduxjs/toolkit";

export const apiStoreReducer = createSlice({
    name: 'apiWizelineStore',
    initialState: [],
    reducers: {
      
    }
});

export const {

} = apiStoreReducer.actions;

export default apiStoreReducer.reducer;